# VProfile on Containers

## Overview
This project demonstrates the containerization of the VProfile application using Docker.

## Docker Setup
1. Navigate to the `docker` directory.
2. Run `docker-compose up -d` to start the containers.
